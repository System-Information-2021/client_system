const INITIAL_STATE = {
    products: [],
    cart: [],
    currentItem: null
}

const shoppingReducer = (state = INITIAL_STATE, { type, payload }) => {
    switch (type) {


        default:
            return state
    }
}

export default shoppingReducer